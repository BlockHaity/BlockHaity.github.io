name: Pages

on:
  push:
    branches:
      - main  # default branch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # If your repository depends on submodule, please see: https://github.com/actions/checkout
          submodules: recursive
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          # Examples: 20, 18.19, >=16.20.2, lts/Iron, lts/Hydrogen, *, latest, current, node
          # Ref: https://github.com/actions/setup-node#supported-version-syntax
          node-version: '20'
      - name: Cache NPM dependencies
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.OS }}-npm-cache
          restore-keys: |
            ${{ runner.OS }}-npm-cache
      - name: Install Dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public
  deploy-to-ghpages:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
  deploy-to-ftp:
    needs: build  
    runs-on: ubuntu-latest
    steps:
     - name: Delete files in wwwroot directory on FTP server
       uses: StephanThierry/ftp-delete-action@v2.1
       with:
         host: ${{ secrets.FTP_HOST }}
         user: ${{ secrets.FTP_USERNAME }}
         password: ${{ secrets.FTP_PASSWORD }}
         remoteFiles: "*"  # 删除wwwroot目录下所有文件
         remoteDirectories: "/wwwroot/"  # 指定wwwroot目录
         workingDir: "."  # 工作目录设置为wwwroot的上级目录
     - name: test dir
       run:  ls
#     - name: Push files to wwwroot directory on FTP server
#       uses: sebastianpopp/ftp-action@releases/v2
#       with:
#         host: ${{ secrets.FTP_HOST }}
#         user: ${{ secrets.FTP_USERNAME }}
#         password: ${{ secrets.FTP_PASSWORD }}
#         localDir: ./public  # 指定本地要上传的目录，根目录'.'表示仓库根目录
#         remoteDir: /wwwroot/  # 指定FTP服务器上的远程目录